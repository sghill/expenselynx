<% if @receipt.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@receipt.errors.count, "error") %> prohibited this receipt from being saved:</h2>

    <ul>
    <% @receipt.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<%= form_for(@receipt, :html => {:multipart => true}) do |f| %>
  <div id="receipt_fields">
    <div id="receipt_fields_core">
      <div class="field">
        <%= f.label :store_name %><br />
        <%= f.text_field :store_name, :placeholder => 'Store Name' %>
      </div>
      <div class="field">
        <%= f.label :purchase_date %><br />
        <%= f.text_field :purchase_date, :placeholder => 'Purchased On', :size => 11 %>
      </div>
      <div class="field">
        <%= f.label :total %><br />
        <%= f.number_field :total, :placeholder => 'Total', :size => 10 %>
      </div>
    </div>
    <div id="receipt_fields_metadata"><br />
      <div class="field">
        <%= f.label :expensable, 'Expensable?' %>
        <%= f.check_box :expensable %>
        <%= f.label :expensed, 'Expensed?' %>
        <%= f.check_box :expensed %>
      </div>
      <div class="field">
        <%= f.label :guests %><br />
        <% unless current_user.participants.empty? %>
          <table class="center_object">
            <% current_user.participants.each_with_index do |participant, i| %>
              <% names = @receipt.participants.collect{ |n| n.name } %>
              <% if i % 2 == 0 %>
                <tr>
              <% end %>
                  <td>
                    <%= check_box_tag "old_participants[#{i}]", 
                        participant.name,     
                        names.include?(participant.name), 
                        {:name => "old_participants[]"} %>
                    <%= label_tag "old_participants[#{i}]", 
                        participant.name %>
                  </td>
              <% unless i % 2 == 0  %>
                </tr>
              <% end %>
            <% end %>
          </table>
        <% end %>
        <p><strong>add new</strong>
        <%= text_field_tag :participant_names, nil, :placeholder => 'Art Vandelay, SG Hill' %></p>
      </div>
      <div class="field">
        <%= f.label :note %><br />
        <%= f.text_field :note, :placeholder => 'breakfast, cell phone' %>
      </div>
    </div>
    <div class="actions clearer">
     <%= f.submit %>
    </div>
  </div>
<% end %>
