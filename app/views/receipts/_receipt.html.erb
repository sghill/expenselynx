<tr>
  <td><%= receipt.purchase_date.to_date.to_s(:short) %></td>
  <td><%= receipt.store.name %></td>
  <td><%= link_to number_to_currency(receipt.total), receipt %></td>
  <td><%= receipt.expensable? ? 'Y' : 'N' %></td>
  <td><%= receipt.expensed? ? 'Y' : 'N' %></td>
</tr>
